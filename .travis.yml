sudo: required

language: java

services:
  - docker

branches:
  only:
    - master
    - stable

install: true

script:
  - ./gradlew --no-daemon clean gretl:classes
  - ./gradlew --no-daemon gretl:test gretl:dbTest
  - cd runtimeImage/