/**
 * Validates already-resolved local or SFTP-backed publication target paths.
 * URI parsing and filesystem initialization remain outside the remote updater.
 */
package ch.so.agi.gretl.steps.publisher.out.updateremote.target;
