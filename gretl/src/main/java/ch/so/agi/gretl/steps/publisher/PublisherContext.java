package ch.so.agi.gretl.steps.publisher;

/**
 * Responsibility: hold runtime state shared by modules during one publication
 * run, such as resolved paths, timestamps, settings, and service adapters.
 */
public class PublisherContext {
}
