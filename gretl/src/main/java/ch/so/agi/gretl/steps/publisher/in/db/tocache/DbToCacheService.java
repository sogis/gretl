package ch.so.agi.gretl.steps.publisher.in.db.tocache;

/**
 * Responsibility: coordinate database dataset discovery and export so selected
 * database content becomes cached transfer files for later publication steps.
 */
public class DbToCacheService {
}
