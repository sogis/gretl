/**
 * Creates or updates the target folder for a data ident by seeding a remote
 * temp stage from the current publication, pushing locally created artifacts
 * into that stage, and promoting the stage to {@code aktuell}.
 */
package ch.so.agi.gretl.steps.publisher.out.updateremote;
