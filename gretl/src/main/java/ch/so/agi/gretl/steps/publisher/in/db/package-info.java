/**
 * Reads matching datasets from an ili2db-compatible database schema and writes
 * transfer files plus dataset metadata into the Publisher temp dir.
 */
package ch.so.agi.gretl.steps.publisher.in.db;
