package ch.so.agi.gretl.steps.publisher;

/**
 * Responsibility: represent one publication-status record written to metadata
 * sinks, including dataset identifier, part identifier, model, timestamp, and
 * available formats.
 */
public class PublishedRecord {
}
