/**
 * Loads publication JSON from a configured HTTP-accessible S3 bucket and writes
 * the first section matching a data identifier to a local output file.
 */
package ch.so.agi.gretl.steps.publisher.out.metainfo.json;
