/**
 * Builds publication archives in the Publisher cache by combining transfer data
 * with validation artifacts and keeping unchanged regional data when required.
 */
package ch.so.agi.gretl.steps.publisher.cache.append.data;
