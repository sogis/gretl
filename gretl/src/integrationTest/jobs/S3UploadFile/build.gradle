import ch.so.agi.gretl.tasks.*

apply plugin: 'ch.so.agi.gretl'

def awsAccessKey = findProperty("awsAccessKey")
def awsSecretKey = findProperty("awsSecretKey")
def awsBucketName = findProperty("awsBucketName")

defaultTasks 'fileupload'

/*
task fileupload(type: S3Upload){
	awsAccessKey = ""
	awsSecretKey = ""
    sourceFile = file("./docs/bar.txt")
    endPoint = "https://s3.amazonaws.com/";
    region = "eu-central-1"
	bucketName = "ch.so.agi.gretl.test"
	acl = "PublicRead"
}
*/

task fileupload() {
    doLast {
        println awsAccessKey
        println awsSecretKey
        println awsBucketName
    }
}