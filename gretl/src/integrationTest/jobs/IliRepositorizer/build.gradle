import ch.so.agi.gretl.tasks.*

apply plugin: 'ch.so.agi.gretl'

defaultTasks 'createIliModels'

task createIliModels(type: IliRepositorizer) {
    modelsDir = file("models/")
    dataFile = "ilimodels.xml"
}
