import ch.so.agi.gretl.tasks.*

apply plugin: 'ch.so.agi.gretl'

defaultTasks 'compressFile'

tasks.register('compressFile', Gzip) {
    dataFile = file("./planregister.xml")
    gzipFile = file("./planregister.xml.gz")
}
